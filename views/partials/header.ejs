<h1>The Jobs EJS Application</h1>
<% if (errors && errors.length > 0) { errors.forEach((err) => { %>
<div>Error: <%= err %></div>
<% }) } %> <% if (info && info.length > 0) { info.forEach((msg) => { %>
<div>Info: <%= msg %></div>
<% }) } %>
<hr />
